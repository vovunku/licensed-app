import React from 'react';
import { ClaimRequest, AuthRequest, SignatureRequest, SismoConnectResponse, SismoConnectClientConfig, SismoConnectClient } from '@sismo-core/sismo-connect-client';
export * from '@sismo-core/sismo-connect-client';

type ButtonProps = {
    appId?: string;
    claim?: ClaimRequest;
    claims?: ClaimRequest[];
    auth?: AuthRequest;
    auths?: AuthRequest[];
    signature?: SignatureRequest;
    onResponse?: (response: SismoConnectResponse) => void;
    onResponseBytes?: (responseBytes: string) => void;
    config?: SismoConnectClientConfig;
    callbackPath?: string;
    callbackUrl?: string;
    namespace?: string;
    verifying?: boolean;
    loading?: boolean;
    text?: string;
    overrideStyle?: React.CSSProperties;
};
declare const SismoConnectButton: ({ appId, claims, claim, auths, auth, signature, onResponse, onResponseBytes, config, callbackPath, callbackUrl, namespace, verifying, text, loading, overrideStyle, }: ButtonProps) => JSX.Element;

type SismoConnectHook = {
    response: SismoConnectResponse | null;
    responseBytes: string;
    sismoConnect: SismoConnectClient;
};
type SismoConnectProps = {
    config: SismoConnectClientConfig;
};
declare const useSismoConnect: ({ config }: SismoConnectProps) => SismoConnectHook;

export { SismoConnectButton, SismoConnectHook, SismoConnectProps, useSismoConnect };
