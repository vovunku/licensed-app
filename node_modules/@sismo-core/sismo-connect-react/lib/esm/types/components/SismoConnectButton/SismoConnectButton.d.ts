import React from "react";
import './SismoConnectButton.css';
import { SismoConnectResponse, SismoConnectClientConfig, ClaimRequest, AuthRequest, SignatureRequest } from "@sismo-core/sismo-connect-client";
type ButtonProps = {
    appId?: string;
    claim?: ClaimRequest;
    claims?: ClaimRequest[];
    auth?: AuthRequest;
    auths?: AuthRequest[];
    signature?: SignatureRequest;
    onResponse?: (response: SismoConnectResponse) => void;
    onResponseBytes?: (responseBytes: string) => void;
    config?: SismoConnectClientConfig;
    callbackPath?: string;
    callbackUrl?: string;
    namespace?: string;
    verifying?: boolean;
    loading?: boolean;
    text?: string;
    overrideStyle?: React.CSSProperties;
};
export declare const SismoConnectButton: ({ appId, claims, claim, auths, auth, signature, onResponse, onResponseBytes, config, callbackPath, callbackUrl, namespace, verifying, text, loading, overrideStyle, }: ButtonProps) => JSX.Element;
export {};
